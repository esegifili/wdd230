const button = document.querySelector('button')
const input = document.querySelector('#favchap')
const list = documentquerySelector('#list')

button.addEventListener('click', () => {
  if (input.value != '') {
    alert('Enter Chapter and Verse')
  }
})
